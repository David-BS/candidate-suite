name: Tests

# Everyday test gate (TST-1) — level L0 on every PR / push to main:
# stdlib + python-docx + markdown logic. PDF-conversion tests are skipped here
# (binaries not installed), keeping this gate fast. The heavier levels run at
# release time, where it matters: L1 (wkhtmltopdf) and L2 (LibreOffice) are
# driven by the `tests` job in release.yml, which blocks the build if it fails.
# The job result is always visible as a check; a per-run table is written to the
# run Summary (stdlib parser, no extra action).

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      # L0 (always): the runtime packages the Claude environment provides.
      - name: Install test dependencies
        run: pip install pytest python-docx markdown

      - name: Run tests
        run: pytest tests/ --junitxml=report.xml

      - name: Write test summary
        if: always()
        run: |
          python - <<'PY'
          import os
          import xml.etree.ElementTree as ET

          out = os.environ.get("GITHUB_STEP_SUMMARY")
          lines = ["## Test results", ""]
          if not os.path.exists("report.xml"):
              lines.append("No report produced.")
          else:
              root = ET.parse("report.xml").getroot()
              suites = root.findall("testsuite") or [root]
              total = failures = errors = skipped = 0
              failed = []
              for s in suites:
                  total += int(s.get("tests", 0))
                  failures += int(s.get("failures", 0))
                  errors += int(s.get("errors", 0))
                  skipped += int(s.get("skipped", 0))
                  for tc in s.findall("testcase"):
                      if tc.find("failure") is not None or tc.find("error") is not None:
                          failed.append(f"{tc.get('classname', '')}::{tc.get('name', '')}")
              passed = total - failures - errors - skipped
              lines += [
                  "| Passed | Failed | Errors | Skipped | Total |",
                  "|---|---|---|---|---|",
                  f"| {passed} | {failures} | {errors} | {skipped} | {total} |",
                  "",
              ]
              lines.append("All tests passed." if not failed else "### Failures")
              lines += [f"- `{n}`" for n in failed]
          with open(out, "a", encoding="utf-8") as f:
              f.write("\n".join(lines) + "\n")
          PY
