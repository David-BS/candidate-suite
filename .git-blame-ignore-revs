# Commits that only reformat code (no logic change) are listed here so that
# `git blame` skips them and points at the meaningful change instead.
# GitHub uses this file automatically. Locally, enable it once with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# After committing the "adopt ruff format" change, paste its full SHA below:
# <reformat-commit-sha>
b6420b69697018863ea4f44680cf769f760b277d
